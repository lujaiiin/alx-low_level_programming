#include "main.h"

/**
 *reverse_array - function
 *@a: value
 *@n: value
 *Return: always
 */

void reverse_array(int *a, int n)
{
	int i, j;
	int s;

	j = 0;

	for (i = n; i >= 0; i--)
	{
		s[j] = a[i];
		j++;
	}
}
